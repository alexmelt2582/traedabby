# Handoff: TRAE SOLO CN Enhancer account suite

## Current state

This repository is a local, non-invasive enhancement assistant for `TRAE SOLO CN`
on Windows. It injects a panel into the running Electron renderer through CDP and
keeps account authentication snapshots on the local machine.

Implemented and user-confirmed:

- CDP-based UI injection with account backup, account switching, seamless OAuth
  login, and traditional fake-logout login.
- Transactional account switching: refresh the target token, stop TRAE precisely,
  replace only authentication keys, restart, verify identity, and roll back on
  failure.
- Seamless login: browser OAuth without closing TRAE.
- Fake logout: back up the current account, preserve non-authentication state,
  reopen TRAE at the login page, detect the new identity, save it, and restore the
  original account on cancel, timeout, or failure.
- Encrypted import/export: selected accounts are exported with `gzip`,
  `AES-256-GCM`, and `scrypt`. Imports validate and decrypt the file, deduplicate by
  stable identity, update existing backups, and add new accounts without switching
  the current login.
- Cold start through `scripts/start.cmd` was verified by the user with TRAE fully
  closed. The current TRAE session was started through that script.

Current repository state:

- Project path: `D:\Me\副业\Trae多账号协同`
- Branch: `main`
- Latest commit: `5cc9384 feat: add encrypted account import and export`
- Previous feature commit: `8dfd2c3 feat: add traditional TRAE fake-logout login`
- Worktree should be clean after the handoff document is committed separately.
- No Git remote is configured.

Live verification at handoff time:

- Local daemon: `127.0.0.1:47834`
- CDP port: `9334`
- CDP connected: yes
- Saved accounts: 3
- Current account: `用户8436325759`
- `npm test`: 26 tests passed
- `npm run check`: passed

Important code facts:

- Login state is read from
  `%APPDATA%\TRAE SOLO CN\User\globalStorage\storage.json`.
- The authoritative login key is `iCubeAuthInfo://icube.cloudide`.
- A complete snapshot also needs the matching `iCubeAuthInfo://icube-dc:*`
  device key, `iCubeAuthInfo://usertag`, and `iCubeServerData://*`.
- Entitlement data may be either a standalone
  `iCubeEntitlementInfo://icube.cloudide` key or embedded as `entitlementInfo`
  inside `iCubeServerData://icube.cloudide`. Both forms must remain supported.
- Do not write `state.vscdb` for account switching. The current Trae version
  restores iCube login state from `storage.json`.
- Non-authentication keys such as workspaces, settings, extensions, and window
  state must always be preserved.

Primary modules:

- `src/launcher.js`: starts TRAE with CDP, starts the daemon, injects the UI.
- `src/daemon.js`: loopback API, account operations, switch transaction, OAuth and
  fake-logout route wiring.
- `src/cdp/client.js`: CDP connection, injection, reconnect loop, live identity.
- `src/lib/accounts.js`: account index, backup, export selection, deduplicated
  import.
- `src/lib/secure-transfer.js`: encrypted account export envelope and validation.
- `src/lib/trae-storage.js`: authentication snapshot extraction, validation,
  identity parsing, and compatibility normalization.
- `src/lib/storage-transaction.js`: atomic snapshot apply, clear, and rollback.
- `src/lib/trae-refresh.js`: token refresh used before account switching.
- `src/lib/trae-oauth.js`: seamless login.
- `src/lib/trae-fake-logout.js`: fake-logout state machine and re-login detection.
- `src/ui/inject.js`: renderer panel and all user-facing dialogs.

## Next steps

Priority order:

1. Add persistent recovery for an interrupted fake-logout flow. If the daemon
   restarts after authentication keys were cleared, it currently loses the
   in-memory session. The next version should persist enough session state to
   resume detection or safely restore the original account.
2. Add mutual exclusion between seamless OAuth and fake logout if both can be
   started close together. Account switching already conflicts with fake logout.
3. Confirm whether the next product phase includes check-in, quota refresh, or
   keep-alive behavior. Those are not implemented yet.
4. Decide later whether to add packaging, an installer, autostart, a Git remote,
   and release automation. None of those exist yet.

Do not start a new feature without user confirmation. Each feature must be tested
by the user before commit.

## Working commands

This Codex environment keeps Git metadata in `.git-meta` because the standard
`.git` directory has been read-only for the command executor:

```powershell
git --git-dir=.git-meta --work-tree=. status --short
git --git-dir=.git-meta --work-tree=. log --oneline -5
npm test
npm run check
node src/launcher.js
```

Health check:

```powershell
Invoke-RestMethod http://127.0.0.1:47834/api/health
```

Authenticated local API calls need the token from `data/api-token`. Never print or
log the token value.

Restarting the daemon after source changes may require stopping the exact daemon
PID reported by `/api/health`, then running `node src/launcher.js` again. Do not
kill all Node or Electron processes.

## Safety boundaries

- Bind services to `127.0.0.1` only.
- Never log or expose access tokens, refresh tokens, cookies, private keys,
  passwords, or complete authentication snapshots.
- Do not modify the official TRAE installation or `app.asar`.
- Only manage processes whose executable path matches the configured TRAE SOLO CN
  installation.
- Do not run Cockpit Tools and this assistant's automatic token refresh flow at
  the same time. They can rotate each other's refresh tokens.
- Import/export files are encrypted. Never add a plaintext export fallback.
- Use UTF-8 without BOM for source and JSON files. Keep `.cmd` files ASCII/CRLF.

## Relevant artifacts

- `README.md`: user-facing capabilities and development commands.
- `AGENTS.md`: project rules and safety boundaries.
- `scripts/start.cmd`: verified Windows cold-start entry point.
- `test/secure-transfer.test.js`: encryption, password, tamper, and validation
  coverage.
- `test/trae-fake-logout.test.js`: successful login detection, cancellation
  restore, and timeout restore.
- `test/storage-transaction.test.js`: authentication-only clearing and rollback.
- `test/trae-storage.test.js`: embedded entitlement compatibility and identity
  extraction.
- Recent commits:
  - `5cc9384 feat: add encrypted account import and export`
  - `8dfd2c3 feat: add traditional TRAE fake-logout login`
  - `82dfd30 feat: add transactional TRAE account switching`

## Suggested skills

- `Code`: use the project workflow for implementing and verifying the next
  feature.
- `windows-shell`: use when validating `scripts/start.cmd`, process boundaries, or
  Windows encoding outside Codex.
- `neat-freak`: use after the next completed feature to align README, AGENTS, and
  runtime facts.
- `git-essentials`: use when resuming the `.git-meta` commit workflow.
- `grill-me`: use before implementation if check-in, quota, packaging, or
  cross-machine transfer scope is still ambiguous.
